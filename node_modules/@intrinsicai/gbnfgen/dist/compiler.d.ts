import { Grammar } from "./grammar.js";
export declare function toGrammar(iface: Interface): Grammar;
export type PropertyType = string | {
    reference: string;
    isArray: boolean;
};
export interface InterfaceProperty {
    name: string;
    type: PropertyType;
}
export interface Interface {
    name: string;
    properties: Array<InterfaceProperty>;
}
/**
 * Async variant of main compilation function, targeting {@link Grammar} type from raw TypeScript interface source code.
 * @param source
 * @returns
 */
export declare function compile(source: string, rootType: string): Promise<Grammar>;
/**
 * Sync variant of main compilation function, targeting {@link Grammar} type from raw TypeScript interface source code.
 * @param source
 * @returns
 */
export declare function compileSync(source: string, rootType: string): Grammar;
